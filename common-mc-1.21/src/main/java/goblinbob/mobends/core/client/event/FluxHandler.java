package goblinbob.mobends.core.client.event;

/**
 * Handles flux/reactive system events.
 * Platform-specific implementations can add event listener methods.
 */
public class FluxHandler
{
    // Platform implementations add event listener methods
}
