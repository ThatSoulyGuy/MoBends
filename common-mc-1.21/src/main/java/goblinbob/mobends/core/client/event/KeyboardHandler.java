package goblinbob.mobends.core.client.event;

/**
 * Handles keyboard input events.
 * Platform-specific implementations can add event listener methods.
 */
public class KeyboardHandler
{
    /**
     * Registers key mappings.
     * Platform implementations should override this.
     * @param event The key mapping registration event
     */
    public static void registerKeyMappings(Object event)
    {
        // Platform implementations add key bindings
    }
}
